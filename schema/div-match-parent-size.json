{
  "type": "object",
  "description": "Размер элемента подстраивается под родительский элемент.",
  "properties": {
    "weight": {
      "$ref": "common.json#/positive_number",
      "description": "Вес при распределении свободного места между элементами с типом размера `match_parent` внутри элемента. Если вес не указан, то элементы поделят место поровну."
    },
    "type": {
      "type": "string",
      "enum": [
        "match_parent"
      ]
    }
  },
  "required": [
    "type"
  ]
}
