{
  "type": "object",
  "$description": "translations.json#/div_keyboard_input",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "keyboard"
      ]
    },
    "keyboard_type": {
      "type": "string",
      "enum": [
        "single_line_text", "multi_line_text", "phone", "number", "email", "uri", "decimal"
      ],
      "default_value": "multi_line_text",
      "$description": "translations.json#/div_input_keyboard_type"
    }
  },
  "required": [
    "type"
  ]
}
