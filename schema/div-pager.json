{
  "java_extends": "DivBlockWithId",
  "description": "Пейджер. Содержит горизонтальный набор карточек, которые можно скроллить постранично. Показывает основную страницу и начало следующей.",
  "generate_swift_optional_arguments": false,
  "allOf": [
    {
      "$ref": "div-base.json"
    },
    {
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "pager"
          ]
        },
        "layout_mode": {
          "$ref": "div-pager-layout-mode.json",
          "description": "Тип вычисления ширины основной страницы:<li>`fixed` — из фиксированной ширины следующей страницы `neighbour_page_width`;</li><li>`percentage` — из процентного значения `page_width`.</li>"
        },
        "item_spacing": {
          "$ref": "div-fixed-size.json",
          "default_value": "{\"type\": \"fixed\",\"value\":0}",
          "description": "Расстояние между элементами."
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "div.json"
          },
          "minItems": 1,
          "clientMinItems": 1,
          "description": "Элементы пейджера. Варианты постраничного перехода можно осуществить с помощью:<li>`div-action://set_current_item?id=&item=` — задать текущую страницу с порядковым номером `item` внутри элемента, с заданным `id`;</li><li>`div-action://set_next_item?id=[&overflow={clamp|ring}]` — перейти на следующую страницу внутри элемента, с заданным `id`;</li><li>`div-action://set_previous_item?id=[&overflow={clamp|ring}]` — перейти на предыдущую страницу внутри элемента, с заданным `id`.</li></p><p>Опциональный параметр `overflow` позволяет задать навигацию при достижении первого или последнего элемента:<li>`clamp` — переход остановится на пограничном элементе;</li><li>`ring` — переход в начало или конец, в зависимости от текущего элемента.</li></p><p>По умолчанию, `clamp`."
        },
        "orientation": {
          "type": "string",
          "default_value": "horizontal",
          "enum": [
            "horizontal", "vertical"
          ],
          "description": "Ориентация пейджера."
        },
        "restrict_parent_scroll": {
          "$ref": "common.json#/boolean_int",
          "default_value": "false",
          "description": "При включенном параметре пейджер не будет передавать жест скролла родительскому элементу.",
          "platforms": [
            "android"
          ]
        },
        "default_item": {
          "$ref": "common.json#/non_negative_integer",
          "default_value": "0",
          "description": "Порядковый номер элемента пейджера, который будет открываться по умолчанию."
        }
      }
    }
  ],
  "required": [
    "type",
    "items",
    "layout_mode"
  ]
}
