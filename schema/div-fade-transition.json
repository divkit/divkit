{
  "description": "Анимация прозрачности.",
  "allOf": [
    {
      "$ref": "div-transition-base.json"
    },
    {
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "fade"
          ]
        },
        "alpha": {
          "type": "number",
          "constraint": "number >= 0.0 && number <= 1.0",
          "default_value": "0.0",
          "description": "Значение альфа-канала, с которого начнется появление или на котором закончится исчезновение элемента."
        }
      }
    }
  ],
  "required": [
    "type"
  ]
}
