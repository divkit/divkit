{
  "description": "Анимация перемещения.",
  "allOf": [
    {
      "$ref": "div-transition-base.json"
    },
    {
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "slide"
          ]
        },
        "edge": {
          "type": "string",
          "enum": [
            "left",
            "top",
            "right",
            "bottom"
          ],
          "default_value": "bottom",
          "description": "Край родительского элемента для одного из типов действий:<li>откуда элемент будет двигаться при появлении;</li><li>куда элемент будет двигаться при исчезновении.</li>"
        },
        "distance":{
          "$ref": "div-dimension.json",
          "description": "Фиксированное значение смещения, с которого начнется появление или на котором закончится исчезновение элемента. Если значение не указано, то берется расстояние до выбранного края родительского элемента."
        }
      }
    }
  ],
  "required": [
    "type"
  ]
}
