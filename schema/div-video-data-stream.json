{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "stream"
      ]
    },
    "url": {
      "$ref": "common.json#/url",
      "$description": "translations.json#/div_video_stream_url"
    }
  },
  "required": [
    "type",
    "url"
  ],
  "$description": "translations.json#/div_video_stream"
}
