{
  "type": "string",
  "enum": [
    "visible",
    "invisible",
    "gone"
  ],
  "description": "Видимость элемента:<li>`visible` — элемент виден;</li><li>`invisible` — элемент не виден, но под него зарезервировано место;</li><li>`gone` — элемент не виден, место не зарезервировано.</li>"
}
