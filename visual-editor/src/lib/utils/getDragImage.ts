/* import { componentIcon } from './componentIcon';

export function getDragImage(type: string): HTMLImageElement | undefined {
    const src = componentIcon(type);

    if (!src) {
        return;
    }

    const img = new Image();
    img.src = src;
    return img;
}
 */
